<table class='table table-hover borderless'>
  <tbody>
    <% teams.each do |team| %>
      <tr>
        <td><%= image_tag(team.image_url, size: '50') %></td>
        <td><h4><%= link_to(team.name, team) %></h4></td>
        <td>
          <%= render partial: 'subscribe_star', locals: { team: team } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
